"use client";
  //the code above will make the rendering of that component happen on the client.
  //we must declare it when we are using the hooks or anything similar we must use the use client.

import styles from './page.module.css';


export default function Home() {
return (
  <main>
    test

  </main>
)
}